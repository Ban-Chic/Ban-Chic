import { useEffect } from "react";
// import { getPerfumeList } from "../../../api/Api";

function CategorySearch() {
  useEffect(() => {
    // getPerfumeList("Winter").then((data) => console.log(data));
  }, []);
  return <></>;
}

export default CategorySearch;
