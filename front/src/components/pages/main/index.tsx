import styled from "styled-components";

function MainPage() {
  return (
    <>
      <main>
        <SSpanContainer>
          메인
        </SSpanContainer>
      </main>
    </>
  );
}

const SSpanContainer = styled.span`
  
  color: white;
`;

export default MainPage;

